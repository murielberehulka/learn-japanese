{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "RUN",
            "type": "shell",
            "command": "cargo run --release"
        },
        {
            "label": "COMPILE",
            "type": "shell",
            "command": "cargo run --bin compile --release"
        }
    ]
}